int method() {
    var = 1 + 1;
    var = 10 - 10;
    var = 'a' + 'b';
    var = 10 * 10;
    var = 20 % 100.0;
    var = getOne() + getTwo();
    var = getOne() + 100;
    var = 50 * 10 + 20;
    var = (50 + 10);
    var = (50 + 10) * 3;
    var = (100 % 10) + (10 + 10);
    var = -10;
    var = !false;
    var = !true;
    var = -10 * 20;
    var = (-10 + 20) * (10 + 20);
    var = -(10 + 10);
    var = -(-10);
    var = !!false;
    var = ---10;
    var = 1;
    var = getOne();
    var = -(10 + 10);
    var = (10 + 10) / getOne();
    var = ((10 + 10)) / (((200)));
    varOne = varTwo;
    var [ x ^ y ^ z ] = 10 - 20;
    var [ 1 ^ 2 ^ 3 ] = 10 - 20;
    var [ 10 * 3 + 10 ^ 2 ^ 3 ^ getValue() ^ i ] = 30 + 10 * list[5];
    var [x] = a + list[10+10 ^ 20 ^ 30] * list[10*10 ^ 30 ^ 40];
}